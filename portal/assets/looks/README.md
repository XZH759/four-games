# Portal look asset slots (STEP 2)

Current mapping reuses existing companion icons. Replace with dedicated look PNGs later without changing layout.

| look_key | Current asset |
|----------|----------------|
| `explorer` | `/companions/ava/icon.png` |
| `mage` | `/companions/gladys/icon.png` |
| `pilot` | `/companions/diana/icon.png` |
| `artist` | `/companions/eileen/icon.png` |

Stored in Neon `users.profile.avatar_key` and `users.profile.badge_key`.
